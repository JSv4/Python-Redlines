"""Clippit redline engine binary for python-redlines.

This package ships only the compiled engine binary for a single platform,
under ``_binaries/``. It is installed automatically via::

    pip install python-redlines[clippit]

It is not meant to be imported directly; ``python_redlines.ClippitEngine``
locates the binary here at runtime.
"""
